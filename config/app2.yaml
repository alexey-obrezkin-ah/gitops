nameOverride: app2
# Use a valid nginx tag
image:
  tag: "1.27.2"
# Keep custom liveness probe path, chart default readiness remains /
livenessProbe:
  enabled: true
  httpGet:
    path: /hello
    port: http
